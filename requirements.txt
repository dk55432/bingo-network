# Bingo Network — runtime dependencies.
#
# Direct deps only (no transitive junk). Generated by hand from what the app
# actually imports (main.py, bingo_scan.py, 01_CNN_refactor/*).
#
# NOTE: torch + torchvision are pinned in requirements-cnn.txt (CPU-only
# builds from PyTorch's CPU wheel index, so they work on Macs and the Orange
# Pi and don't drag in CUDA packages).  Install with:
#   pip install -r requirements.txt -r requirements-cnn.txt
#
# The CNN reader needs torch; the tesseract ("default") reader works without it.

fastapi==0.141.1
uvicorn==0.52.1
python-multipart==0.0.32
pydantic==2.13.4
Jinja2==3.1.6
websockets==17.0.1

numpy==2.4.6
pillow==12.3.0
opencv-python-headless==5.0.0.93
pytesseract==0.3.13
scikit-learn==1.9.0
scipy==1.17.1
